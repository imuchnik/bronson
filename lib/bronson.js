// Generated by CoffeeScript 1.3.3
(function() {
  var Client, HTTPController, IO, Room, httpController;

  IO = require('socket.io');

  Client = require('./client').Client;

  Room = require('./room').Room;

  HTTPController = require('./httpcontroller').HTTPController;

  httpController = null;

  exports.setHost = function(host, port) {
    httpController = new HTTPController(host, port);
    return exports;
  };

  exports.listen = function(port, options) {
    if (options == null) {
      options = {};
    }
    exports.io = IO.listen(port, options);
    exports.io.sockets.on('connection', function(socket) {
      return new Client(socket, httpController);
    });
    return exports;
  };

}).call(this);
